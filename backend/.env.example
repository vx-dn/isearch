# Environment Variables for Receipt Search Application

# === AWS Configuration ===
AWS_REGION=ap-southeast-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# === DynamoDB Tables ===
RECEIPTS_TABLE_NAME=receipt-search-receipts
USERS_TABLE_NAME=receipt-search-users

# === S3 Configuration ===
S3_BUCKET_NAME=receipt-search-images

# === Cognito Configuration (Optional for development) ===
COGNITO_USER_POOL_ID=ap-southeast-1_XXXXXXXXX
COGNITO_CLIENT_ID=your-client-id
COGNITO_CLIENT_SECRET=your-client-secret

# === Meilisearch Configuration (Optional) ===
MEILISEARCH_HOST=http://localhost:7700
MEILISEARCH_API_KEY=your-meilisearch-api-key
MEILISEARCH_INDEX_NAME=receipts

# === SQS Configuration ===
SQS_RECEIPT_PROCESSING_QUEUE=receipt-processing-queue

# === Application Configuration ===
PORT=8000
DEBUG=true
LOG_LEVEL=INFO

# === JWT Configuration (Development only) ===
JWT_SECRET_KEY=your-super-secret-jwt-key-change-in-production
JWT_ALGORITHM=HS256

# === File Upload Limits ===
MAX_FILE_SIZE_MB=10

# === Database Connection (If using RDS for development) ===
# DATABASE_URL=postgresql://user:password@localhost:5432/receipt_search

# === Redis Configuration (For caching/sessions) ===
# REDIS_URL=redis://localhost:6379/0

# === Email Configuration (For notifications) ===
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# === External API Keys ===
# GOOGLE_VISION_API_KEY=your-google-vision-api-key
# AZURE_COMPUTER_VISION_KEY=your-azure-cv-key
