# Lambda function dependencies - optimized for AWS Lambda deployment
# Include base runtime dependencies
-r ../requirements.txt

# Lambda-specific runtime adapter
mangum>=0.17.0

# Lambda optimizations (replace heavy packages with lighter alternatives)
# Note: boto3 and botocore are included in Lambda runtime, but specified for local dev

# Production monitoring for Lambda
psutil>=7.0.0,<8.0.0

# Database drivers (only if actually using PostgreSQL in Lambda)
# psycopg2-binary>=2.9.7  # Uncomment if using PostgreSQL
