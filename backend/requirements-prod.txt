# Production dependencies - optimized for deployment
# Include only essential runtime dependencies
-r requirements.txt

# Production WSGI server (alternative to uvicorn for some deployments)
gunicorn>=22.0.0,<23.0.0

# Production monitoring (optional)
prometheus-client>=0.22.0,<1.0.0

# Health checks
psutil>=7.0.0,<8.0.0